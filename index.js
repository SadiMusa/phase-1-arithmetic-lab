// Helpers.js

// Variables
const num1 = 31;
const num2 = 2;
const num3 = 14;
const num4 = 10;
const num5 = 8;
const num6 = 20;

// Arithmetic operations
const multiply = num1 * num2; // Should equal 62
const random = Math.floor(Math.random() * 100) + 1; // Generates a random integer greater than 0
const mod = num3 % num4; // Should result in a remainder equal to 4
const max = Math.max(num1, num2, num3, num4, num5, num6); // Finds the highest number in the set; should return 20


